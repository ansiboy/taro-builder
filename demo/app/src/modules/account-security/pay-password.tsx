import Taro, { Config } from "@tarojs/taro";
import { View } from "@tarojs/components";

export default class PayPassword extends Taro.Component {
    config: Config = {
        navigationBarTitleText: "支付密码"
    }

    render() {
        return <View className="page">

        </View>
    }
}